# Non-secret target for production-mode Vite tooling and local build previews.
# Static production hosting still needs a host-side /api reverse proxy.
# Override in .env.production.local or the process environment.
API_PROXY_TARGET=https://of-acme-salary-dashboard-service-production.up.railway.app
